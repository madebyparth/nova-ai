# Deployment

Current deployment is intended for local-network use.

1. Start FastAPI.
2. Connect ESP32 to the same Wi-Fi.
3. Configure the ESP32 with the server IP.
4. Upload firmware.

Before open-sourcing:
- Remove Wi-Fi credentials.
- Rotate API keys.
- Move secrets into config files.
